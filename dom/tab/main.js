'use strict';

function kuku(){
  
  for ( let i = 1; 1 <= i,i <= 9; i++){
    var list = [];
    for (let j = 1; 1 <= j,j <= 9; j++){
      
      let result  = i * j; 
      list.push(result);
      
      //   let resultList = result.map(Number);
      //   console.log(resultList);
      // }
    }
    console.log(list);
  };
};

kuku();